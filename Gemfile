source 'http://rubygems.org'  # Originally https, no s at the end now

gem 'rails', '3.2.21'

group :development do		# added
	gem 'sqlite3', '1.3.5'	# added
						
end							# added 

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

#gem 'sqlite3' # commented this


# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.4' # Originally 3.2.3
  gem 'coffee-rails', '~> 3.2.2' # Originally 3.2.1

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '>= 1.2.3'	# Originally 1.0.3
end

gem 'jquery-rails', '~> 2.0.0'
#gem 'jquery-rails', '2.0.0'	# Originally only 'jquery-rails', 
								#then gem 'jquery-rails', '2.0.0' didn't work, so 
								# resorted to line 28.

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'
